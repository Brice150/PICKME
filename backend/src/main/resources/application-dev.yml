# Dev Configuration
# Every setting can be overridden by an environment variable, the values below being the ones of a
# local PostgreSQL installation.
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:pickme}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:root}
